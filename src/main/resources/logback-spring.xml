<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ✅ Base path for application logs -->
    <property name="LOG_PATH" value="logs/RadiusApp" />


    <!-- log file path for production-->
    <!-- <property name="LOG_PATH" value="/opt/apache-tomcat-10.1.24/logs/RadiusApp" />-->
    <property name="LOG_FILE" value="${LOG_PATH}/RadiusApp.log"/>

    <!-- Rolling File Appender for the entire app -->
    <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/RadiusApp.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>14</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root Logger: Logs everything to both console and file -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="APP_FILE"/>
    </root>

</configuration>
